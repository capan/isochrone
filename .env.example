# Copy to .env and change the password before exposing anything publicly.
PGPASSWORD=password

# Which city schema the backend queries — must match the schema
# import_city.sh created (dashes become underscores: greater_london).
CITY=berlin
