node_modules/
dist/
.env
.env.*
!.env.example

# OSM extracts and derived data — 1.6GB of berlin.osm alone.
# Regenerate with overpass/osm-imports/import_city.sh
*.osm
*.osm.pbf
*.zip
osm_db/*/

# run artifacts
performance.txt
*.log

.DS_Store
__MACOSX/

# claude artifacts — working memory, deliberately not published
HANDOFF.md
decisions/
tickets/

# agent orchestration
CLAUDE.md
.claude/

# Caddy access logs: the directory is tracked for the local mount, the logs are not.
caddy-logs/*
!caddy-logs/README.md
