# Caddy access logs land here.
# Tracked so local `docker compose up` has a directory to mount; on the server
# CADDY_LOGS_DIR points at /opt/caddy-logs instead. Log files themselves are
# gitignored — see .gitignore.
